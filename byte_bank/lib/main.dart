import 'package:flutter/material.dart';

//runApp hace que lo que pongamos dentro se ejecute en una app, y no en la terminal
void main() => runApp(const Text("Hello World", textDirection: TextDirection.ltr));
